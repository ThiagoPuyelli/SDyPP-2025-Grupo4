# !!!

* Usar `terraform apply` solo una vez en este directorio al inicio del proyecto, las ips no deberian ser destruidas

* `provider.tf, variables.tf y terraform.tfvars` deben ser los mismos en el proyecto raiz 